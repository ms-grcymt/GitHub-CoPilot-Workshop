using Newtonsoft.Json;

namespace Contoso.Banking.Models
{
    public class Users
    {
        // Create a model for the Users table in json format
        // The model should have the following properties: name, surname, id, email, password, accountnumber, balance, transactions
    }
}
